app = "smc-scanner-bot"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[env]
  PYTHONUNBUFFERED = "1"
  PYTHONIOENCODING = "utf-8"

# Cấu hình chạy Machine như một Cronjob (mỗi 15 phút tại phút 00, 15, 30, 45)
[machine]
  schedule = "0,15,30,45 * * * *"
